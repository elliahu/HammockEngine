#version 450

layout (local_size_x = 16, local_size_y = 16) in;
layout (binding = 0, rgba8) uniform writeonly image2D storageImage;

layout (binding = 1) uniform UBO
{
    vec4 cameraPosition;
} ubo;

layout (std140, binding = 2) buffer Scene
{
    vec4 color;
} storageBuffer;

void main(){
    ivec2 dim = imageSize(storageImage);
    vec2 uv = vec2(gl_GlobalInvocationID.xy) / dim;

    imageStore(storageImage, ivec2(gl_GlobalInvocationID.xy), storageBuffer.color);
}